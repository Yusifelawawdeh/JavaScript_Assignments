var answer = prompt("what programming language is also the name of a snake");

  if( answer.toLowerCase() === "python"){
  document.write("Corecto Mundo Buddy");
} else {
  document.write("Nope... that appears to be incorrect.");
}